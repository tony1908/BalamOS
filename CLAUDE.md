# Model routing

- Think, plan, and validate with Opus 4.8.
- Execute every task with OpenCode DeepSeek V4.1 Flash, using exactly:
  `opencode run --auto --pure -m opencode-go/deepseek-v4.1-flash --variant low "<task>"`
- If `opencode` is not present, stop.
